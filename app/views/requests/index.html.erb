<h1>Requests</h1>

<table>
  <tr>
    <td>URL</td>
    <td>名前</td>
    <td>メールアドレス</td>
  </tr>
  <% @requests.each do |request| %>
  <tr>
    <td><%= request.url %></td>
    <td><%= request.name %></td>
    <td><%= request.email %></td>
  </tr>
  <% end %>
</table>
